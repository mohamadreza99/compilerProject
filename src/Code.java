public class Code {
    OpCode opCode;
    String op1;
    String op2;
    String res;
}
enum OpCode{add, sub, mult, div, je, jne, jg, jge, jl, jle, jmp, mov, neg, not, xor, cmp, pop, push, call, ret, jz, jnz, inc, and, or}
